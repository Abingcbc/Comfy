<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:id="@+id/team_task"
        android:background="@drawable/fake_cardview"
        android:elevation="3dp">

    <TextView
            android:id="@+id/team_task_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="aaaaaa"
            android:textSize="48dp"
            android:layout_marginStart="10dp"/>

    <cn.yiiguxing.compositionavatar.CompositionAvatarView
        android:layout_width="@dimen/firstpage_icon"
        android:layout_height="@dimen/firstpage_icon"
        android:id="@+id/composition_avatar"
        android:layout_alignParentRight="true"/>

</RelativeLayout>